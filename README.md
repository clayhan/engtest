# Orderbook

Primary technologies used:

- Next.js
- MaterialUI

## How to use

Clone down the repository and then run:

```bash
npm i
npm run dev
```

Once the app is running, it can be accessed via http://localhost:3000/
The order book specifically can be found at http://localhost:3000/orders
